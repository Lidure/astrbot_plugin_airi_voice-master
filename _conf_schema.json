{
  "trigger_mode": {
    "description": "触发模式",
    "type": "string",
    "hint": "direct: 直接输入关键词；prefix: 必须用 #voice 前缀；llm: 仅注册 LLM 工具，由大模型调用语音功能",
    "default": "direct",
    "options": ["direct", "prefix", "llm"]
  },
  "admin_mode": {
    "description": "管理命令权限模式",
    "type": "string",
    "hint": "all: 所有人可用；admin: 仅平台管理员；whitelist: 仅白名单用户",
    "default": "whitelist",
    "options": ["all", "admin", "whitelist"]
  },
  "admin_whitelist": {
    "description": "管理员白名单",
    "type": "text",
    "hint": "每行一个用户 ID 或昵称，仅当 admin_mode 为 whitelist 时生效",
    "default": ""
  },
  "extra_voice_pool": {
    "description": "额外语音文件池（网页上传）",
    "hint": "上传 mp3/wav 等文件，文件名（去后缀）作为触发关键词。支持多文件上传。",
    "type": "file",
    "default": []
  },
  "llm_select_mode": {
    "description": "LLM 语音选择模式",
    "type": "string",
    "hint": "list: 先列出全部语音供 LLM 选择；keyword: LLM 根据用户给出的关键词筛选语音",
    "default": "list",
    "options": ["list", "keyword"]
  },
  "auto_catchphrase": {
    "description": "口癖/关键词自动补发语音（只对 bot 自己发出的文本生效）",
    "type": "object",
    "default": {
      "enabled": false,
      "mode": "first",
      "max_per_message": 2,
      "map": {}
    },
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "是否开启此功能",
        "default": false
      },
      "mode": {
        "type": "string",
        "description": "匹配到关键词后如何选择语音",
        "default": "first",
        "options": ["first", "random", "all"],
        "hint": "first: 只发第一个；random: 随机发一个；all: 全部发（小心刷屏）"
      },
      "max_per_message": {
        "type": "integer",
        "description": "单条消息最多补发几条语音（防刷屏）",
        "default": 2,
        "minimum": 1,
        "maximum": 5
      },
      "map": {
        "type": "object",
        "description": "关键词 → 语音名称列表 的映射",
        "default": {},
        "hint": "key: 口癖关键词（支持包含标点），value: 语音名称数组（必须已在 voice_map 中存在）\n示例：\n\"打卡啦摩托\": [\"打卡啦摩托语音\"],\n\"哎嘿~\": [\"哎嘿短\", \"哎嘿长版\"]",
        "additionalProperties": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    }
  }
}